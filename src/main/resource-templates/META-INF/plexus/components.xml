<component-set>
    <components>
        <component>
            <role>
                org.apache.maven.lifecycle.mapping.LifecycleMapping
            </role>
            <role-hint>helm</role-hint>
            <implementation>
                org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
            </implementation>
            <configuration>
                <lifecycles>
                    <lifecycle>
                        <id>default</id>
                        <phases>
                            <initialize>${project.groupId}:${project.artifactId}:${project.version}:download-helm</initialize>
                            <generate-sources>${project.groupId}:${project.artifactId}:${project.version}:generate-helm-sources</generate-sources>
                            <process-sources>${project.groupId}:${project.artifactId}:${project.version}:dependency-update</process-sources>
                            <package>
                                ${project.groupId}:${project.artifactId}:${project.version}:package
                            </package>
                            <install>org.apache.maven.plugins:maven-install-plugin:install</install>
                            <deploy>
                                ${project.groupId}:${project.artifactId}:${project.version}:deploy-helm,
                                org.apache.maven.plugins:maven-deploy-plugin:deploy
                            </deploy>
                        </phases>
                    </lifecycle>
                </lifecycles>
            </configuration>
        </component>
        <component>
            <role>org.apache.maven.artifact.handler.ArtifactHandler</role>
            <role-hint>helm</role-hint>
            <implementation>
                org.apache.maven.artifact.handler.DefaultArtifactHandler
            </implementation>
            <configuration>
                <type>helm</type>
                <extension>helm</extension>
                <language>helm</language>
                <addedToClasspath>false</addedToClasspath>
            </configuration>
        </component>
    </components>
</component-set>